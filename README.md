# Argument_Mining_10K

In order to run the code you need to register with ArgumenText (https://api.argumentsearch.com/en/api_registration). Copy the file _credentials.py to credentials.py in the code. Edit it by adding your ArgumenText credentials.